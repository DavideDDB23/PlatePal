import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color.fromRGBO(206, 206, 206, 1);

  static const Color backgroundGradientStart = Color.fromRGBO(206, 206, 206, 1);
  static const Color backgroundGradientEnd = Color.fromRGBO(234, 234, 234, 1);

  static const Color cardBackground = Color.fromRGBO(234, 233, 238, 1);
  static const Color primaryText = Colors.black;
  static const Color secondaryText = Colors.grey; // For less important text

  static const Color proteinColor = Color.fromRGBO(33, 124, 122, 1); // Teal
  static const Color carbsColor = Color.fromRGBO(224, 122, 79, 1); // Orange
  static const Color fatsColor = Color.fromRGBO(212, 172, 58, 1);     // Yellow

  static const Color activeTab = Colors.black;
}